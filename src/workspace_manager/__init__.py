"""workspaceManager — an open-source, reproducible macOS file-organization toolkit.

Three LLM-driven agents:
  * janitor        — flag large + stale files/folders for review (never deletes)
  * download_sorter — classify each new download and file it away
  * reporter        — document file-system state + improvement opportunities
"""

__version__ = "0.1.0"
